// Takes the parsed comments, file path, generates unique id and stores the comments in todo_new.json and todo_old.json
//
// @param {Array} comments - The comments to be stored
// @param {string} filePath - The path of the file
// @returns {void}
//
export default function generateReport() {
    return;
};