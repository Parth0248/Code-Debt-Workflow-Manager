// on detecting submit event, the current choice from the dropdown is passed to the handleSubmit function


const handleSubmit = (cardTemplate, task) => {
  cardTemplate.body = cardTemplate.body.map(item => {
    
  });
   
};
  
  export default handleSubmit;
  