import { ActivityResponse, IActivityRequest } from "adaptivecards/lib/activity-request";
export declare abstract class ChannelAdapter {
    abstract sendRequestAsync(request: IActivityRequest): Promise<ActivityResponse>;
}
